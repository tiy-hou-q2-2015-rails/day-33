<%= simple_form_for @contact do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :email, disabled: !@contact.new_record? %>
  <%= f.input :phone %>
  <%= f.input :company_name %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
